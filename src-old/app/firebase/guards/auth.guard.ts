// TODO: Implement @angular/fire auth guard using inject(Auth) + route data as needed.
// Keep logic minimal and avoid direct Firebase SDK calls outside DI.
export const firebaseAuthGuard = () => true;
